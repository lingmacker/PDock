# Distribute the Initial Release with Developer ID

The initial release will be signed with Developer ID, notarized, and distributed outside the Mac App Store. This avoids making App Store sandbox entitlement and review acceptance a launch dependency for a utility whose core behavior requires cross-application Accessibility control and Screen Recording access; a separate Mac App Store version may be evaluated later.
