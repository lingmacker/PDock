# Domain Docs

This is a single-context repository.

Before exploring or changing the project, read the root `CONTEXT.md` and the relevant decisions under `docs/adr/`. Use the glossary's canonical terms in issues, specifications, code, and tests; avoid the synonyms explicitly rejected by the glossary.

If a proposed change conflicts with an ADR, surface the conflict rather than silently overriding the recorded decision.
